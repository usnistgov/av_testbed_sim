[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "av_tested_sim"
authors = [{name = "Kootbally", email = "zeid.kootbally@nist.gov"}]
dynamic = ["version", "description"]
